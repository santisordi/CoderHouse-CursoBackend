import Assert from 'assert';
import { nameF}

const assert = Assert.strict 

descrbie ('nameFormatter', () => {
    it('Should format the name correctly', () => {
        
        nameFormatter

    })
});